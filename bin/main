#!/usr/bin/env node

var Pusher = require('../lib/main').Pusher;

var pusher = new Pusher({
  name: 'pusher-hook',
  debug: true,
  // appId: 'appId',
  // key: 'key',
  // secret: 'secret'
});

pusher.start();